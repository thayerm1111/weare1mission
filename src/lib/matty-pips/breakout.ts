/**
 * MATTY PIPS — DEPRECATED (mp-2.0.0). Breakout phases are now classified by
 * the reaction engine (reaction.ts: ACCEPTED_BREAK / BREAK_RETEST /
 * MOMENTUM_CONTINUATION / EXPANSION_BREAKOUT) and graded by quality.ts
 * (BREAKOUT_WEAK/VALID/STRONG), with the expansion decision path in
 * momentum.ts. Kept as an empty module so old imports fail loudly.
 */
export {};
