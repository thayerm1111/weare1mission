/**
 * MATTY PIPS — DEPRECATED (mp-2.0.0). Location is now decided by the level
 * map's active node (levels.ts) and the reaction engine (reaction.ts): the
 * old static location states were part of the timeframe-alignment design the
 * owner corrected. Kept as an empty module so old imports fail loudly at
 * build time rather than silently resolving.
 */
export {};
