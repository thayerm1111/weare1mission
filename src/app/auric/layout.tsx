export const dynamic = "force-dynamic";
/** Pass-through: /auric now redirects into the portal (see page.tsx). */
export default function AuricLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
