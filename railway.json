{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'worker service - no Next build needed'"
  },
  "deploy": {
    "startCommand": "npm run worker",
    "restartPolicyType": "ALWAYS"
  }
}
