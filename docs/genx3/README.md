# GENX 3.0

Branch: `genx-3.0`. GENX 2.0 (current production on `main`) stays untouched and remains the
rollback target until GENX 3.0 passes shadow mode.

- `phase1-audit.md` — current-system audit (architecture, signal trace, Flow contract, risks).
